coverage
node_modules
dist
tsconfig.tsbuildinfo
tsconfig.build.tsbuildinfo
.claude
/.idea/
.DS_Store

# Example npm lockfiles churn when switching between local/published mcp-nest
examples/**/package-lock.json

# Root lockfile is package-lock.json only; bun test does not read bun.lock and a second
# lockfile goes stale on every Dependabot bump (Dependabot edits package-lock.json only).
/bun.lock
