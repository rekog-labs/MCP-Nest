# Link the local workspace packages to satisfy in-repo dependencies (e.g.
# @rekog/mcp-nest-auth's peer on @rekog/mcp-nest) instead of resolving them
# from the registry. pnpm-specific keys; npm/bun ignore them.
link-workspace-packages=true
prefer-workspace-packages=true
